# Hero products ship as final CAD, inserted in post. The world registers the
# CAD as external truth — it never stores the model itself. Replace the paths
# and set current/approved when real exports land.

amara-ref:
  kind: image
  source: local
  path: ../refs/amara_ref_v1.png
  current: pending
  approved: false
  note: "Approved character reference for Amara — pick a canon-clean frame
    from the culled 4-ups (scar LEFT, braided ponytail). Rides along as
    --cref / omni-reference; image refs hold identity and chirality far
    better than prompt text."

pulse-arc-cad:
  kind: cad-export
  source: local
  path: ../refs/pulse-arc/
  current: pending
  approved: false
  note: "Pulse Arc AI watch — final CAD not yet delivered; watch is in-iteration."

orbit-home-cad:
  kind: cad-export
  source: local
  path: ../refs/orbit-home/
  current: pending
  approved: false
  note: "Orbit Home AI speaker (~18cm tall) — final CAD pending."

vista-air-cad:
  kind: cad-export
  source: local
  path: ../refs/vista-air/
  current: pending
  approved: false
  note: "Vista Air AI goggles — final CAD pending."
